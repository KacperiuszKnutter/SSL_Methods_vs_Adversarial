# batch_size: 256 # Zwiększ do 2048-4096 dla ImageNet (wymaga dużej pamięci VRAM)
# lr: 0.3 # Skaluj liniowo: lr * (batch_size / 256)
# crop_size: 32 # Ustaw 224 dla ImageNet / ImageNet-100
# gaussian_blur: enabled: False # Włącz (True) tylko dla obrazów o wysokiej rozdzielczości (> 64px)

method: simclr
dataset: cifar10
backbone: resnet50
num_classes: 10

name: simclr-final-resnet50

checkpoint: "pytorch_hub"
use_projector: false

backbone_args:
  zero_init_residual: true

proj_hidden_dim: 2048
proj_output_dim: 256
temperature: 0.2

max_epochs: 100
optimizer: lars #sgd
lars: true
lr: 0.4  #4.8
weight_decay:  1e-4 #0.000001
classifier_lr: 0.5
exclude_bias_n_norm_lars: true
accumulate_grad_batches: 1
extra_optimizer_args:
  momentum: 0.9

scheduler: warmup_cosine
scheduler_interval: step
min_lr: 0.0
warmup_start_lr: 0.0
warmup_epochs: 10
lr_decay_steps: null

augmentations:
  rrc:
    enabled: True
    crop_size: 32
  color_jitter:
    prob: 0.8
  grayscale:
    prob: 0.2
  gaussian_blur:
    prob: 0.5 # Symetryczne rozmycie dla SimCLR[cite: 12]
  horizontal_flip:
    prob: 0.5

num_large_crops: 2
num_small_crops: 0
num_crops_per_aug: [2, 0]

batch_size: 4096 #256
num_workers: 8
data_dir: ./datasets
train_dir: null
val_dir: null

gpus: "0"

# Ścieżki relatywne względem katalogu głównego
report_dir: project/models_out/reports
figures_dir: project/models_out/figures
embeddings_dir: project/models_out/embeddings
checkpoint_dir: project/models_out/checkpoints/mine

knn_k: 20
knn_temperature: 0.1
knn_distance_fx: cosine

linear_max_iter: 1000
linear_c: 1.0




